timestamp :

ISO-8601 format. Required for time-based anomaly detection.

serviceName :

Identifies which microservice generated the log.

logLevel :

INFO / WARN / ERROR. Used for severity filtering.

message :

Actual log content. Used for clustering.

traceId :

Used to correlate events across services.

host :

Machine that generated log.